import { takeEvery, all } from 'redux-saga/effects';

// Constants
import {
    SESSION_REQUESTED
} from '~actions/index';

// Sagas Functions

export default function* root(): Generator<unknown, void, unknown> {
    yield all([
    ]);
}
